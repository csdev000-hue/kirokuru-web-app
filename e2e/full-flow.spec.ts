import { expect, test } from "@playwright/test";
test("E2E-FULL-01 Login → Human Reviews → Ticket → Kanban done", async ({ page, context }) => {
 const token = process.env.E2E_SESSION_TOKEN; if (!token) throw new Error("Missing test session");
 await context.addCookies([{ name: "authjs.session-token", value: token, domain: "127.0.0.1", path: "/", httpOnly: true, sameSite: "Lax" }]);
 await page.goto("/dashboard"); await expect(page.getByText("Owner A", {exact:true})).toBeVisible();
 await page.goto("/organizations/new"); await page.getByLabel("組織名",{exact:true}).fill("Full flow organization"); await page.getByRole("button",{name:"作成",exact:true}).click();
 await expect(page.getByRole("heading",{name:"Full flow organization",exact:true})).toBeVisible();
 await page.getByRole("link",{name:"プロジェクトを作成",exact:true}).click();await page.getByLabel("プロジェクト名").fill("Full flow project");await page.getByRole("button",{name:"作成",exact:true}).click();
 await expect(page.getByRole("heading",{name:"Full flow project",exact:true})).toBeVisible();const project={id:page.url().split("/").at(-1)!};
 await page.goto(`/projects/${project.id}/meetings/new`);await page.getByLabel("会議名",{exact:true}).fill("Full flow meeting");await page.getByLabel("開催日時（日本時間）",{exact:true}).fill("2026-09-21T10:00");await page.getByRole("button",{name:"会議を作成",exact:true}).click();
 await expect(page.getByRole("heading",{name:"Full flow meeting",exact:true})).toBeVisible();const meeting={id:page.url().split("/").at(-1)!};
 const transcript=page.getByRole("form",{name:"文字起こしを追加",exact:true});await transcript.getByLabel("話者名",{exact:true}).fill("QA speaker");await transcript.getByLabel("開始秒",{exact:true}).fill("12");await transcript.getByLabel("終了秒",{exact:true}).fill("18");await transcript.getByLabel("発言本文",{exact:true}).fill("API仕様を確認して更新する。");await transcript.getByRole("button",{name:"文字起こしを追加",exact:true}).click();
 await expect(page.locator("p.user-content").filter({hasText:"API仕様を確認して更新する。"})).toBeVisible();
 const noTickets=async()=>expect((await (await context.request.get(`/api/projects/${project.id}/tickets`)).json()).data).toHaveLength(0);
 await page.goto(`/meetings/${meeting.id}/minutes`); await page.getByRole("button", { name: "AI議事録を生成", exact: true }).click(); await expect(page.getByRole("heading", { name: "Version 1 · review" })).toBeVisible(); await noTickets(); await page.getByLabel("要約",{exact:true}).fill("人がレビューして修正した要約"); await page.getByRole("button",{name:"議事録を保存",exact:true}).click(); await expect(page.getByRole("button",{name:"内容を確認して承認"})).toBeEnabled(); await page.getByRole("button", { name: "内容を確認して承認" }).click(); await expect(page.getByRole("heading", { name: "Version 1 · approved" })).toBeVisible();
 await page.getByRole("link", { name: "AIチケット候補を確認" }).click(); await expect(page.getByText("AIチケット候補はまだありません", { exact: false })).toBeVisible(); await page.getByRole("button", { name: "AIチケット候補を生成", exact: true }).click(); await expect(page.getByText("2件の候補", { exact: true })).toBeVisible();
 await noTickets();
 const first = page.getByRole("article", { name: "API仕様を更新する", exact: true });
 await expect(first.getByRole("button", { name: "正式チケットとして登録", exact: true })).toHaveCount(0);
 await first.getByLabel("優先度", { exact: true }).selectOption("high"); await first.getByRole("button", { name: "候補を保存" }).click(); await expect(first.getByRole("button", { name: "承認", exact: true })).toBeEnabled(); await first.getByRole("button", { name: "承認", exact: true }).click();
 await noTickets();
 await first.getByRole("button", { name: "正式チケットとして登録", exact: true }).click(); await expect(first.getByText("状態: 登録済み候補", { exact: true })).toBeVisible(); await first.getByRole("link", { name: "正式チケットを見る", exact: true }).click();
 await expect(page.getByRole("heading", { name: "API仕様を更新する", exact: true })).toBeVisible(); const ticketUrl = page.url();
 const source = page.getByRole("region", { name: "作成元" }); await expect(source.getByText("このチケットは会議から作成されました")).toBeVisible(); await source.getByRole("link", { name: "元の議事録 Version 1" }).click(); await expect(page.getByRole("heading", { name: "Version 1 · approved" })).toBeVisible();
 await page.goto(ticketUrl); await page.getByRole("link", { name: "元の候補・根拠発言" }).click(); await expect(page.getByRole("heading", { name: "チケット候補の確認" })).toBeVisible(); await page.getByRole("link", { name: /発言を確認/ }).click(); await expect(page.locator("li:target")).toContainText("API仕様を確認して更新する。");
 await page.goto(`/projects/${project.id}/tickets`); await expect(page.getByRole("link", { name: "API仕様を更新する", exact: true })).toBeVisible(); await page.goto(`/projects/${project.id}/board`); await expect(page.getByRole("region", { name: "todo", exact: true }).getByRole("link", { name: "API仕様を更新する", exact: true })).toBeVisible();
 await page.getByLabel("状態変更: API仕様を更新する",{exact:true}).selectOption("in_progress");await expect(page.getByRole("region",{name:"in_progress",exact:true}).getByRole("link",{name:"API仕様を更新する",exact:true})).toBeVisible();await expect(page.getByLabel("状態変更: API仕様を更新する",{exact:true})).toBeEnabled();await page.getByLabel("状態変更: API仕様を更新する",{exact:true}).selectOption("done");await expect(page.getByRole("region",{name:"done",exact:true}).getByRole("link",{name:"API仕様を更新する",exact:true})).toBeVisible();
 await expect(page.getByLabel("状態変更: API仕様を更新する",{exact:true})).toBeEnabled();await page.waitForLoadState("networkidle");
 for(const width of [390,768,1280]){await page.setViewportSize({width,height:900});for(const path of [`/projects/${project.id}/tickets`,`/projects/${project.id}/board`,`/meetings/${meeting.id}`,`/meetings/${meeting.id}/minutes`,`/meetings/${meeting.id}/ticket-candidates`]){await page.goto(path);await expect(page.getByRole("heading",{level:1})).toBeVisible();expect(await page.evaluate(()=>document.documentElement.scrollWidth<=window.innerWidth+1)).toBe(true);}}
 await page.goto(`/meetings/${meeting.id}/ticket-candidates`); const second = page.getByRole("article", { name: "API仕様を確認する", exact: true }); await second.getByLabel("優先度", { exact: true }).selectOption("medium"); await second.getByRole("button", { name: "候補を保存" }).click(); await expect(second.getByRole("button", { name: "承認", exact: true })).toBeEnabled(); await second.getByRole("button", { name: "承認", exact: true }).click();
 const bulkSection = page.getByRole("region", { name: "候補の一括登録" }); await bulkSection.getByRole("checkbox", { name: "API仕様を確認する", exact: true }).check(); await bulkSection.getByRole("button", { name: "選択した候補を正式登録", exact: true }).click(); await expect(second.getByText("状態: 登録済み候補", { exact: true })).toBeVisible();
 const tickets = await (await context.request.get(`/api/projects/${project.id}/tickets`)).json(); expect(tickets.data).toHaveLength(2);
});
